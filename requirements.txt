# Demo-runtime deps. The GPU stack (unsloth/bitsandbytes/peft/accelerate/
# transformers/trl/xformers) is installed in the Dockerfile so wheel selection
# can match the actual CUDA toolchain. Don't pin those here.

gymnasium
numpy
matplotlib
streamlit==1.32.0
plotly==5.19.0
huggingface_hub
python-dotenv
pyyaml
